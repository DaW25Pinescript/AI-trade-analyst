# Macro Risk Officer — Python dependencies
# Install: pip install -r macro_risk_officer/requirements.txt

# HTTP client (data source ingestion)
httpx>=0.27.0

# Config file parsing
pyyaml>=6.0

# Data validation (shared with ai_analyst — ensure version compatibility)
pydantic>=2.5.0

# Price outcome fetching (MRO-P4 — soft dependency, YFinanceClient fails gracefully if absent)
yfinance>=0.2.40

# Modal — remote compute for the macro ingestion feeder worker.
# Install to run: python -m modal run macro_risk_officer/modal_macro_worker.py
# Optional for unit tests: pure functions in modal_macro_worker.py are
# importable without modal (Modal app block is guarded by try/except ImportError).
modal>=0.68.0

# Testing
pytest>=8.0.0
pytest-cov>=4.0.0
